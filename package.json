{
  "name": "craftfolio-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "copy:ui": "rsync -av --exclude='node_modules' --exclude='.git' --exclude='dist' ../craftfolio/* ./frontend-codebase && cd ./frontend-codebase && npm install",
    "build:ui": "rm -rf dist && cd ./frontend-codebase && npm run build && cp -r dist ../dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "archiver": "^6.0.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "shelljs": "^0.8.5",
    "uuid": "^9.0.1"
  }
}
